import { SearchFilterTwoPipe } from './search-filter-two.pipe';

describe('SearchFilterTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterTwoPipe();
    expect(pipe).toBeTruthy();
  });
});
